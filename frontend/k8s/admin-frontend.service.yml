apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard
  labels:
    app: admin-frontend
spec:
  selector:
    app: admin-frontend
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 5174
